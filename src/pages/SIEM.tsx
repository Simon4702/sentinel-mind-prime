import { Layout } from "@/components/Layout";
import { SIEMDashboard } from "@/components/siem";

const SIEM = () => {
  return (
    <Layout>
      <SIEMDashboard />
    </Layout>
  );
};

export default SIEM;
